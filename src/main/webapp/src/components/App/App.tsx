import * as React from "react";

import "./App.less";
import { Board } from "../Board/Board";

export class App extends React.Component {
    render() {
        return (
            <Board />
        );
    }
}